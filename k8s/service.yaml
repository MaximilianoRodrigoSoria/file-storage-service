apiVersion: v1
kind: Service
metadata:
  name: file-storage-service-service
  namespace: file-storage-service
  labels:
    app.kubernetes.io/name: file-storage-service
spec:
  type: ClusterIP        # El Ingress (ingress.yaml) enruta el tráfico externo al Service
  selector:
    app: file-storage-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
